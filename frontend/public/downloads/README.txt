Place the built Android app here as:
  sawta-guest-house.apk

The website download popup links to /downloads/sawta-guest-house.apk
(see frontend/src/data/site.js -> APP.downloadUrl).

If you host the APK elsewhere (Expo, Google Drive, etc.), just change
APP.downloadUrl in site.js to that external link instead.
